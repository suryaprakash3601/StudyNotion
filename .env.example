## Frontend environment variables (Create React App)
## Copy to `.env` if you want to override defaults.

# Backend API base (defaults to http://localhost:4000/api/v1)
REACT_APP_BASE_URL=http://localhost:4000/api/v1

# Optional: enable Razorpay checkout (leave empty to disable payments)
REACT_APP_RAZORPAY_KEY=

